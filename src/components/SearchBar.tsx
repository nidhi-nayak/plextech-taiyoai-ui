import { FunctionComponent } from "react";
import styles from "./Rectangle 7.module.css";

const SearchBar: FunctionComponent = () => {
  return <div className={styles.desktop1Child} />;
};

export default SearchBar;
